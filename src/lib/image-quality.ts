/**
 * Valores permitidos en `next.config` → `images.qualities`.
 * Usar en `next/image` para fotos (no en logotipos vectoriales / SVG).
 */
export const PHOTO_IMAGE_QUALITY = 90;
/** Portadas y héroes a ancho completo (más peso, máximo detalle). */
export const HERO_IMAGE_QUALITY = 95;
